export async function handle(text, { waId, memory }) {
  return [
    "🏠 **Home Loan**",
    "Please share: property emirate, developer/agent, salary, and down-payment %",
    "I’ll outline eligibility, docs, and transfer timing."
  ].join("\n");
}
