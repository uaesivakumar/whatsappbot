export async function handle(text, { waId, memory }) {
  return [
    "💳 **Credit Cards**",
    "Tell me your company & monthly salary.",
    "I’ll suggest the right ENBD card and share the official link with benefits."
  ].join("\n");
}
