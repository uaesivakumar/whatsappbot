export async function detectIntent(text, intents, { context }) {
  // return { name: "loan" | "greeting" | "unknown", confidence: 0..1 }
}
export async function handleIntent(intentName, text, { intents, memory, waId }) {
  // return a string response
}
